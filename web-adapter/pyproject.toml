[project]
name = "web-adapter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "aiohttp>=3.9",
]

[tool.uv.sources]
shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
