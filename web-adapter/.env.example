# Secrets — never commit the real .env, only this .example file.
# No credentials of its own: doesn't depend on any external service,
# which is exactly why it's the "always available" channel.
MQTT_HOST=mqttbbr
MQTT_PORT=8883
MQTT_USER=bbruser
MQTT_PASSWORD=bbrpassword
MQTT_TLS_ENABLED=true
