# Secrets — never commit the real .env, only this .example file.
# Long polling (no webhook): no public URL or TLS certificate needed.
TELEGRAM_BOT_TOKEN=

MQTT_HOST=mqttbbr
MQTT_PORT=8883
MQTT_USER=bbruser
MQTT_PASSWORD=bbrpassword
MQTT_TLS_ENABLED=true
